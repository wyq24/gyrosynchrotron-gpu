# Internal Tools

This folder is for maintainer-only scripts that are useful during development but are not part of the public API or public quick-start path.

Current contents:

- `phase1_node_mcmc_compare.py`: external-project comparison helper for single-node MCMC step-count studies
